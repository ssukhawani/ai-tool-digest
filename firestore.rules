rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Cloud Functions (Admin SDK bypasses these rules).
    // No client-side access is expected for this pipeline, so deny by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
